#ifndef JIVE_REGALLOC_SHAPED_REGION_PRIVATE_H
#define JIVE_REGALLOC_SHAPED_REGION_PRIVATE_H

#include <jive/regalloc/shaped-region-private.h>

static inline void
jive_shaped_region_add_active_top(jive_shaped_region * self, struct jive_shaped_ssavar * ssavar)
{
	/* placeholder for now */
}

static inline void
jive_shaped_region_remove_active_top(jive_shaped_region * self, struct jive_shaped_ssavar * ssavar)
{
	/* placeholder for now */
}

#endif
